apply: [ ../common.module-template.yaml ]

product:
  type: lib
  platforms: [ jvm, macosArm64 ]

dependencies:
  - ../ifx.protocol.contract
  - ../ifx.service

settings:
  kotlin:
    ksp:
      processors:
        - ../ifx.rpc.ksp
