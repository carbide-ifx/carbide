settings:
  kotlin:
    serialization: enabled
    ksp:
      processors:
        - ./ifx.rpc.index.ksp
dependencies:
  - $kotlin.serialization.json
  - org.jetbrains.kotlinx:kotlinx-coroutines-core:1.10.2: exported
test-dependencies:
  - ./ifx.testing: exported
