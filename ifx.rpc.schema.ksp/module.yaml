apply: [ ../publishing.module-template.yaml ]

description: Canonical KSP service schema model and symbol interpretation for Carbide generators.

product:
  type: jvm/lib

dependencies:
  - com.google.devtools.ksp:symbol-processing-api:2.3.6: compile-only
