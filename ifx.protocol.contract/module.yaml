apply: [ ../publishing.module-template.yaml, ../common.module-template.yaml ]
description: Transport-neutral RPC bindings, descriptors, messages, and interceptors for Carbide.

product:
  type: lib
  platforms: [ jvm, macosArm64 ]

dependencies:
  - ../ifx.service: exported
  - ../ifx.context: exported
  - ../ifx.logging
