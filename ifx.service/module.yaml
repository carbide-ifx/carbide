apply: [ ../publishing.module-template.yaml, ../common.module-template.yaml ]

description: Core Carbide service contracts, lifecycle, fire-and-forget, and response types.

product:
  type: lib
  platforms: [jvm, macosArm64]

dependencies:
  - ../ifx.context: exported
  - ../ifx.logging: exported
  - ../ifx.stdlib: exported
