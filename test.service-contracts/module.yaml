apply: [ ../common.module-template.yaml ]

product:
  type: lib
  platforms: [ jvm, macosArm64 ]

repositories:
  - id: local
    resolve: true
    url: mavenLocal

settings:
  kotlin:
    version: 2.4.10
    ksp:
      processors:
        - ../ifx.contract.ksp

dependencies:
  - ../ifx.service: exported
