apply: [ ../../../../component.module-template.yaml ]

product:
  type: lib
  platforms: [jvm]

settings:
  publishing:
    name: access.person.contract

dependencies:
  - $libs.ifx.service: exported
  - $libs.ifx.stdlib
