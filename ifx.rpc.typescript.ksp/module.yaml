apply: [ ../publishing.module-template.yaml ]

description: KSP processor that generates TypeScript SDK contracts for Carbide services.

product:
  type: jvm/lib

dependencies:
  - com.google.devtools.ksp:symbol-processing-api:2.3.6: compile-only
