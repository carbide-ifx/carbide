apply: [ ../common.module-template.yaml ]
product:
  type: jvm/app

dependencies:
  - ../ifx.host.rsocket
  - ../ifx.proxy-factory.rsocket
  - ../ifx.protocol.contract
  - ../ifx.service

settings:
  jvm:
    mainClass: HostWithInterceptorKt
  kotlin:
    ksp:
      processors:
        - ../ifx.rpc.ksp
