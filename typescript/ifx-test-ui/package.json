{
  "name": "@ifx/test-ui",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node build.mjs",
    "dev": "tsc -p tsconfig.json --noEmit && node dev.mjs"
  },
  "dependencies": {
    "@ifx/rpc-client": "0.0.6",
    "@ifx/rpc-client-rsocket": "0.0.6"
  },
  "devDependencies": {
    "esbuild": "0.25.8",
    "typescript": "7.0.2"
  }
}
