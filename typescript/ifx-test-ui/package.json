{
  "name": "@ifx/test-ui",
  "version": "0.0.9",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node build.mjs",
    "dev": "tsc -p tsconfig.json --noEmit && node dev.mjs"
  },
  "dependencies": {
    "@ifx/rpc-sdk": "0.0.9",
    "@ifx/rpc-sdk-rsocket": "0.0.9"
  },
  "devDependencies": {
    "esbuild": "0.25.8",
    "typescript": "7.0.2"
  }
}
